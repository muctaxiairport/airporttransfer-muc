---
import { Phone, Menu, X } from "@lucide/astro";
import { siteConfig } from "../../config/site";
import LanguageSwitcher from "../ui/LanguageSwitcher.astro";
import type { Locale } from "../../i18n";
import type { Translations } from "../../i18n/types";

interface Props {
  locale: Locale;
  t: Translations;
  alternateUrls: Record<Locale, string>;
}

const { locale, t, alternateUrls } = Astro.props;
const whatsappUrl = `https://wa.me/${siteConfig.whatsapp.number}?text=${encodeURIComponent(t.whatsapp.message)}`;
---

<header id="site-header" class="fixed top-0 left-0 right-0 z-50 transition-[background-color] duration-200 pt-[env(safe-area-inset-top)]">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="relative flex h-20 items-center justify-between">
      <!-- Logo -->
      <a href={locale === "de" ? "/" : `/${locale}/`}>
        <img src="/Logo_Yellow.png" alt={siteConfig.name} class="h-12" />
      </a>

      <!-- Desktop Nav (centered links) -->
      <nav class="hidden md:flex items-center justify-center gap-8 absolute left-1/2 -translate-x-1/2">
        {t.nav.map((item) => (
          <a
            href={locale === "de" ? item.href : `/${locale}${item.href}`}
            class="text-sm font-bold text-white hover:text-taxi-400 transition-colors"
          >
            {item.label}
          </a>
        ))}
      </nav>

      <!-- Right side: Language + Phone -->
      <div class="hidden md:flex items-center gap-4">
        <LanguageSwitcher currentLocale={locale} alternateUrls={alternateUrls} />
        <a
          href={`tel:${siteConfig.phone}`}
          class="inline-flex items-center gap-2 rounded-full bg-taxi-400 px-4 py-2 text-sm font-semibold text-dark hover:bg-taxi-500 transition-colors"
        >
          <Phone class="h-4 w-4" />
          {siteConfig.phoneDisplay}
        </a>
      </div>

      <!-- Mobile Menu Button -->
      <button
        id="mobile-menu-btn"
        class="md:hidden p-2 text-taxi-400 hover:text-white"
        aria-label={t.header.menuOpen}
      >
        <Menu class="h-6 w-6" id="menu-icon-open" />
        <X class="h-6 w-6 hidden" id="menu-icon-close" />
      </button>
    </div>
  </div>

  <!-- Mobile Nav (full-screen overlay) -->
  <div id="mobile-menu" class="hidden md:hidden fixed inset-0 top-0 z-[-1] bg-dark pt-[calc(5rem+env(safe-area-inset-top))] overflow-y-auto">
    <nav class="flex flex-col items-center px-4 py-6 gap-5 text-center border-t border-white/10">
      <!-- Language links -->
      <div class="flex flex-col items-center gap-3">
        <p class="text-xs text-gray-400 uppercase tracking-wider">{t.header.chooseLanguage}</p>
        {[
          { code: "de" as const, label: "Deutsch", flag: "🇩🇪" },
          { code: "en" as const, label: "English", flag: "🇬🇧" },
          { code: "fr" as const, label: "Français", flag: "🇫🇷" },
        ].map((lang) => (
          <a
            href={alternateUrls[lang.code]}
            hreflang={lang.code}
            class:list={[
              "mobile-nav-link flex items-center gap-2 text-sm font-semibold transition-colors",
              lang.code === locale
                ? "text-taxi-400"
                : "text-gray-400 hover:text-white",
            ]}
            aria-current={lang.code === locale ? "page" : undefined}
          >
            <span class="text-base leading-none">{lang.flag}</span>
            <span>{lang.label}</span>
          </a>
        ))}
      </div>
      <div class="w-full border-b border-white/10"></div>
      {t.nav.map((item) => (
        <a
          href={locale === "de" ? item.href : `/${locale}${item.href}`}
          class="mobile-nav-link text-base font-bold text-white hover:text-taxi-400 transition-colors"
        >
          {item.label}
        </a>
      ))}
      <div class="w-full border-b border-white/10"></div>
      <div class="flex flex-col items-center">
        <p class="text-xs text-gray-400 uppercase tracking-wider mb-2">{t.header.whatsapp}</p>
        <a
          href={whatsappUrl}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-2 rounded-full bg-green-500 px-4 py-2 text-sm font-semibold text-white hover:bg-green-600 transition-colors"
        >
          <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor" class="h-4 w-4">
            <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"></path>
          </svg>
          WhatsApp
        </a>
      </div>
      <div class="flex flex-col items-center">
        <p class="text-xs text-gray-400 uppercase tracking-wider mb-2">{t.header.directCall}</p>
        <a
          href={`tel:${siteConfig.phone}`}
          class="inline-flex items-center gap-2 rounded-full bg-taxi-400 px-4 py-2 text-sm font-semibold text-dark hover:bg-taxi-500 transition-colors"
        >
          <Phone class="h-4 w-4" />
          {siteConfig.phoneDisplay}
        </a>
      </div>
    </nav>
  </div>
</header>

<script>
  const btn = document.getElementById("mobile-menu-btn");
  const menu = document.getElementById("mobile-menu");
  const iconOpen = document.getElementById("menu-icon-open");
  const iconClose = document.getElementById("menu-icon-close");
  const header = document.getElementById("site-header");

  function isMenuOpen() {
    return !menu?.classList.contains("hidden");
  }

  function updateHeaderBg() {
    if (isMenuOpen() || window.scrollY > 50) {
      header?.classList.add("bg-dark");
    } else {
      header?.classList.remove("bg-dark");
    }
  }

  function toggleMenu() {
    const opening = !isMenuOpen();
    menu?.classList.toggle("hidden");
    iconOpen?.classList.toggle("hidden");
    iconClose?.classList.toggle("hidden");
    if (opening) {
      document.body.classList.add("overflow-hidden");
    } else {
      document.body.classList.remove("overflow-hidden");
    }
    updateHeaderBg();
  }

  btn?.addEventListener("click", toggleMenu);
  window.addEventListener("scroll", updateHeaderBg);

  document.querySelectorAll(".mobile-nav-link").forEach((link) => {
    link.addEventListener("click", () => {
      menu?.classList.add("hidden");
      iconOpen?.classList.remove("hidden");
      iconClose?.classList.add("hidden");
      document.body.classList.remove("overflow-hidden");
      updateHeaderBg();
    });
  });
</script>
